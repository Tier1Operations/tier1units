/*
 * Author: Nelson Duarte, AACO
 * Function used to handle mouse scroll event
 *
 * Arguments:
 * 0: Control <CONTROL>
 * 1: Change in Z <NUMBER>
 *
 * Return Value:
 * None
 *
 * Example:
 * _this call ace_spectator_fnc_ui_handleMouseZChanged
 *
 * Public: No
 */

//#include "script_component.hpp"
/*
#define FOLLOW_CAMERA_MAX_DISTANCE 5
#define MODE_FOLLOW 2

diag_log "TEST123 HandleMouseZChanged START";
diag_log format ["FOLLOW_CAMERA_MAX_DISTANCE: %1", FOLLOW_CAMERA_MAX_DISTANCE];
diag_log format ["MODE_FOLLOW: %1", MODE_FOLLOW];
diag_log format ["ace_spectator_camMode: %1", ace_spectator_camMode];
diag_log format ["ace_spectator_camMode == 2: %1", ace_spectator_camMode == 2];
diag_log format ["_this: %1", _this];
diag_log format ["_(_this select 1) > 0: %1", (_this select 1) > 0];
diag_log format ["ace_spectator_camDistance: %1", ace_spectator_camDistance];
diag_log format ["(ace_spectator_camDistance - 1) max 0: %1", (ace_spectator_camDistance - 1) max 0];
diag_log format ["(ace_spectator_camDistance + 1) min FOLLOW_CAMERA_MAX_DISTANCE: %1", (ace_spectator_camDistance + 1) min FOLLOW_CAMERA_MAX_DISTANCE];
diag_log "TEST123 HandleMouseZChanged END";



if (ace_spectator_camMode == 2) then {
    if ((_this select 1) > 0) then {
        ace_spectator_camDistance = (ace_spectator_camDistance - 1) max 0;
    } else {
        ace_spectator_camDistance = (ace_spectator_camDistance + 1) min FOLLOW_CAMERA_MAX_DISTANCE;
    };
};
*/